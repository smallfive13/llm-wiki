*
!.gitignore
!AGENTS.md
!wiki-design/
!wiki-design/**

# 工具脚本（wiki-lint 等，RFC-006 + TASK-006 落地）纳入版本管理。
!scripts/
!scripts/**
# Python 构建缓存不跟踪（在 !scripts/** 之后再排除）
scripts/__pycache__/

# 测试 fixture 纳入版本管理。
!tests/
!tests/**
tests/__pycache__/

# wiki skill 可分发副本（SKILL.md / schema / 模板 / README），跨项目安装用，纳入版本管理。
!skill/
!skill/**

# 以后如果在本仓库直接创建真实知识库，默认把 Markdown 正本和人工队列纳入 Git。
!knowledge/
!knowledge/**

# Obsidian 应用配置（每用户/每机器，非知识正本）
knowledge/.obsidian/

# 派生索引和缓存可重建，不作为知识正本跟踪。
knowledge/.wiki/cache.json
knowledge/.wiki/search_index/
knowledge/.wiki/lightrag/
knowledge/maps/graph-data.json
knowledge/maps/knowledge-graph.md
knowledge/maps/graph-insights.md
knowledge/.wiki/id_index.json
knowledge/.wiki/inbox_index.json
knowledge/.wiki/normalized_alias_index.json

# wiki-eval 趋势快照不可重建，显式 --snapshot 时纳入版本管理。
!knowledge/.wiki/eval_history.jsonl

# schema 分发同步元数据是实例正本审计状态，进 Git。
!knowledge/.wiki/schema_sync.json
